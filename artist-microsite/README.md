# Consabor Artist Microsite

Mini scaffold for artist one-page microsite using Next.js, TypeScript and Tailwind.

Quick start:

```bash
cd artist-microsite
npm install
npm run dev
```

Set `DATABASE_URL` and `API_URL` env vars for local API.
